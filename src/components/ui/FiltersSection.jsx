import { SelectedFilters } from "../utils/SelectedFilters";

export const FiltersSection = () => {
  return (
    <section className="filter-section">
      <SelectedFilters />
    </section>
  );
};
